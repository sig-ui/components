// @ts-check

/**
 * Breadcrumb / breadcrumb module for SigUI web components.
 * @module
 */
import { SiguiElement } from "../../../lib/base-element.js";

/**
 * SiguiBreadcrumb custom element class.
 * @extends {SiguiElement}
 */
export class SiguiBreadcrumb extends SiguiElement {
  static observedAttributes = [];
  static componentKey = "breadcrumb";
}
